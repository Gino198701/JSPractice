
let classes = ["math","computer", "coding", "playwright"];

console.log(classes.length);

classes[5]="cypress"

console.log(classes[5]);

console.log(classes.at(3)); // returns the value from given index number



classes.push("javascript") // add new value at the end of the array

console.log(classes);

classes.pop();
console.log(classes); //removing last data from array
