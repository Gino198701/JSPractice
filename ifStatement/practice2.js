let x = 5;

if(x>6)
    console.log("It is bigger"); // if statament without curly braces, will effect only first line
   // console.log("It is not bigger"); 
else
    console.log("It is bigger");
    // since we don't have curly braces second line will run no matter what.

    let age = 18;
    let dlPrice$ = 400;
    let mySavings = 600;

    if(age>=18){
        console.log("You are eligible to apple for driver license");

        if(mySavings>dlPrice$){
            console.log("You can get your driver license today");
            
        }

        
    }